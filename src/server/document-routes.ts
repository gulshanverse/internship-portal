import { Router } from 'express'; import { z } from 'zod'; import { requireRole } from './middleware'; import { authorizeDocumentDownload, issueDocument, listMyDocuments, publishDocument, revokeDocument } from './document-service';
const issue=z.object({studentId:z.string().min(1),internshipId:z.string().min(1).optional(),type:z.enum(['OFFER_LETTER','CERTIFICATE','EXPERIENCE_LETTER','EVALUATION_REPORT','PROJECT_COMPLETION']),storageKey:z.string().min(1).max(500),mimeType:z.string(),sizeBytes:z.number().int().positive()}); export const documentRouter=Router(); documentRouter.get('/student/documents',requireRole('STUDENT'),async(req,res,next)=>{try{res.json({data:await listMyDocuments(req.auth!.user.id)});}catch(e){next(e);}}); documentRouter.get('/student/documents/:id/download',requireRole('STUDENT'),async(req,res,next)=>{try{res.json({data:await authorizeDocumentDownload(req.auth!.user.id,String(req.params.id))});}catch(e){next(e);}}); documentRouter.post('/admin/documents',requireRole('ADMIN'),async(req,res,next)=>{try{res.status(201).json({data:await issueDocument(req.auth!.user.id,issue.parse(req.body))});}catch(e){next(e);}}); documentRouter.post('/admin/documents/:id/publish',requireRole('ADMIN'),async(req,res,next)=>{try{res.json({data:await publishDocument(req.auth!.user.id,String(req.params.id))});}catch(e){next(e);}}); documentRouter.post('/admin/documents/:id/revoke',requireRole('ADMIN'),async(req,res,next)=>{try{res.json({data:await revokeDocument(req.auth!.user.id,String(req.params.id))});}catch(e){next(e);}});
